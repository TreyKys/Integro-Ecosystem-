// Jest test stub — fill in with proper mocks in dev environment
test('createPendingListing writes listing and publishes HCS', async () => {
// TODO: mock admin.firestore(), mock @hashgraph/sdk TopicMessageSubmitTransaction
expect(true).toBe(true);
});

test('buyNow returns signPayload with fundEscrow', async () => {
expect(true).toBe(true);
});
